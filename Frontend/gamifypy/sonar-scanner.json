{
  "name": "sonar-scanner",
  "version": "1.0.0",
  "description": "SonarQube Scanner para el frontend de GamifyPy",
  "main": "index.js",
  "scripts": {
    "sonar": "sonar-scanner",
    "sonar:docker": "docker run --rm --network host -v %cd%:/usr/src sonarsource/sonar-scanner-cli"
  },
  "devDependencies": {
    "sonar-scanner": "^3.1.0"
  }
}
